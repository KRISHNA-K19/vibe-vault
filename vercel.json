{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.0.0 --activate && pnpm install",
  "buildCommand": "SKIP_ENV_VALIDATION=1 pnpm turbo run build --filter=web",
  "outputDirectory": "apps/web/.next"
}
