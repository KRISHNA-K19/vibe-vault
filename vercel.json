{
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@9.0.0 --activate && pnpm install",
  "buildCommand": "cd ../.. && SKIP_ENV_VALIDATION=1 pnpm turbo run build --filter=web",
  "outputDirectory": ".next"
}
